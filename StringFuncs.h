#ifndef _STRINGFUNCS
#define _STRINGFUNCS

#include <stdlib.h>
#include <stdio.h>

int namecmp(char *first_string, char *second_string);
int mystrlen(char *string);
char* toLower(char *string);

#endif
